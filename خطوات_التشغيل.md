# دليل التشغيل الكامل - خطوة بخطوة

## 📋 الخطوة 1: التحقق من تثبيت المكتبات

تأكد من تثبيت جميع المكتبات المطلوبة:

```bash
pip install -r requirements.txt
```

إذا كانت مثبتة بالفعل، يمكنك تخطي هذه الخطوة.

---

## 📋 الخطوة 2: فتح Notebook وتشغيل جميع الخلايا

1. افتح ملف `anasi.ipynb` في Jupyter Notebook أو VS Code
2. **شغّل جميع الخلايا** من البداية حتى النهاية
3. تأكد من أن جميع النماذج تم تدريبها بنجاح:
   - Logistic Regression (lr)
   - Random Forest (rf)
   - HistGradientBoosting (hgb)
   - Ensemble Model

**ملاحظة مهمة:** يجب أن تكون جميع المتغيرات التالية موجودة في الذاكرة:
- `preprocessor`
- `lr`, `rf`, `hgb`
- `w_lr`, `w_rf`, `w_hgb`, `t_ens`
- `X_test`, `y_test`
- `ACTIVE_FEATURES`

---

## 📋 الخطوة 3: حفظ النماذج

بعد التأكد من تدريب جميع النماذج، قم بحفظها:

### الطريقة الأولى: تشغيل السكريبت مباشرة

في خلية جديدة في Notebook، اكتب:

```python
exec(open('save_models.py').read())
```

### الطريقة الثانية: نسخ الكود مباشرة

انسخ محتوى `save_models.py` والصقه في خلية جديدة ثم شغّلها.

**النتيجة المتوقعة:**
```
✓ تم حفظ المعالج المسبق
✓ تم حفظ Logistic Regression
✓ تم حفظ Random Forest
✓ تم حفظ HistGradientBoosting
✓ تم حفظ إعدادات Ensemble
✓ تم حفظ 10000 عينة من بيانات الاختبار
✓ تم حفظ قائمة الميزات النشطة

✅ تم حفظ جميع الملفات بنجاح!
```

---

## 📋 الخطوة 4: التحقق من الملفات المحفوظة

شغّل سكريبت التحقق:

```bash
python check_files.py
```

أو من داخل Notebook:

```python
exec(open('check_files.py').read())
```

**يجب أن ترى:**
```
✅ جميع الملفات موجودة! يمكنك تشغيل Streamlit الآن
```

---

## 📋 الخطوة 5: تشغيل تطبيق Streamlit

في Terminal أو PowerShell، شغّل:

```bash
streamlit run app.py
```

**النتيجة المتوقعة:**
- سيتم فتح المتصفح تلقائياً
- العنوان: `http://localhost:8501`
- ستظهر واجهة التطبيق

---

## 📋 الخطوة 6: استخدام التطبيق

### الصفحة الرئيسية
- نظرة عامة على النموذج
- معلومات أساسية

### صفحة نتائج الاختبار
- عرض جميع المقاييس
- رسوم بيانية (Confusion Matrix, ROC Curve, etc.)
- تقرير تفصيلي

### صفحة التنبؤ بمعاملة جديدة
- إدخال بيانات معاملة
- الحصول على تنبؤ فوري
- عرض مستوى المخاطرة

### صفحة معلومات النموذج
- تفاصيل عن النماذج المستخدمة
- إعدادات Ensemble

---

## ⚠️ حل المشاكل الشائعة

### المشكلة 1: "لم يتم العثور على الملفات المطلوبة"
**الحل:**
- تأكد من تشغيل `save_models.py` من داخل Notebook
- تأكد من أنك في نفس المجلد

### المشكلة 2: "NameError: name 'lr' is not defined"
**الحل:**
- تأكد من تشغيل جميع خلايا Notebook
- تأكد من تدريب جميع النماذج

### المشكلة 3: "ModuleNotFoundError"
**الحل:**
- شغّل: `pip install -r requirements.txt`
- تأكد من تفعيل البيئة الافتراضية الصحيحة

### المشكلة 4: Streamlit لا يفتح المتصفح
**الحل:**
- افتح المتصفح يدوياً واذهب إلى: `http://localhost:8501`
- أو اضغط على الرابط الذي يظهر في Terminal

---

## 📝 ملخص الملفات المطلوبة

بعد الخطوة 3، يجب أن يكون لديك هذه الملفات:

1. ✅ `preprocessor.pkl` - المعالج المسبق
2. ✅ `logistic_regression.pkl` - نموذج Logistic Regression
3. ✅ `random_forest.pkl` - نموذج Random Forest
4. ✅ `hist_gradient_boosting.pkl` - نموذج HistGradientBoosting
5. ✅ `ensemble_config.pkl` - إعدادات Ensemble
6. ✅ `test_data.pkl` - بيانات الاختبار
7. ✅ `active_features.pkl` - قائمة الميزات

---

## 🎯 ترتيب الخطوات السريع

```
1. pip install -r requirements.txt
2. فتح Notebook وتشغيل جميع الخلايا
3. exec(open('save_models.py').read())
4. python check_files.py
5. streamlit run app.py
```

---

**بالتوفيق! 🚀**

